WITH customers AS (
  SELECT ID, WEBSITE FROM "BRIAN_KRANSON"."SQL_SERVER_DEMO_DBO"."ACCOUNT"
  WHERE _FIVETRAN_DELETED = FALSE
  ORDER BY WEBSITE DESC
),

final AS (
  SELECT * FROM customers
)

SELECT * FROM final
